This directory is reserved for PlatformIO firmware tests.

Current protocol and transport tests are implemented in the web project because they are transport-agnostic and can run in CI without BLE hardware.
